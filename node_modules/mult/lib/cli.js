#!/usr/bin/env node
"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
exports.__esModule = true;
var commander_1 = require("commander");
var package_json_1 = __importDefault(require("../package.json"));
var add = require('./add/index.ts')["default"];
var init = require('./init/index.ts')["default"];
var remove = require('./remove/index.ts')["default"];
var script = require('./scripts/index.ts')["default"];
var version = package_json_1["default"].version;
commander_1.program.version(version);
commander_1.program
    .command('init')
    .description('init a new mult config')
    .action(init);
commander_1.program
    .command('add <name> <url>')
    .description('Add new repo to mult')
    .action(add);
commander_1.program
    .command('remove <name>')
    .description('Remove repo from mult')
    .action(remove);
commander_1.program
    .command('script <script>')
    .description('Run a script in each of your mult repos')
    .action(script);
commander_1.program.parse(process.argv);
